package com.example.badgeuse_auto.data

enum class BadgeMode {
    OFFICE,   // mode actuel (multi zones, aide à la personne, etc.)
    DEPOT,     // mode bâtiment / dépôt

    HOME_TRAVEL, //mode pointage après départ maison + temps de route

    MANUAL_ONLY  //mode manuel uniquement
}